// import { render } from "@testing-library/react";
import PokemonsPage from "./PokemonsPage";

describe(PokemonsPage, () => {
  it("simple test", () => {
    // const {} = render(<PokemonsPage/>)
    expect(1).toEqual(1);
  })
})